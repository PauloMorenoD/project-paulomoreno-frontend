import styled from "styled-components";

export const SectionComp = styled.section`
    width: 100vw;
    height: 100vh;
    background-color: rgba(18, 18, 20, .5);
    position: fixed;
    top: 0;
    left: 0;
    display: flex;
    justify-content:center;
    align-items: center;
    z-index: 5;
`